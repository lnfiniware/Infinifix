#!/bin/sh
set -e

mkdir -p /var/lib/infinifix/backups
mkdir -p /var/log/infinifix/reports

if command -v update-desktop-database >/dev/null 2>&1; then
  update-desktop-database /usr/share/applications >/dev/null 2>&1 || true
fi

echo "InfiniFix installed. Run: infinifix doctor"

exit 0
